services:
  - type: web
    name: smiles-finder
    runtime: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: SMILES_CONCURRENCY
        value: "5"
      - key: SMILES_ENGINE
        value: "auto"
